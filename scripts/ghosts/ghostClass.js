class Ghost {
    constructor(x, y, speed) {
        this.x = x;
        this.y = y;
        this.speed = speed;
    }
}

function ghosts() {
    runBlinky();
    // runPinky();
    // runInky();
    // runClyde();
}
